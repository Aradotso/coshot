# Copy to .env.local and `source` it before `swift run` for dev convenience.
# In production, set via the menu bar "Set Cerebras API Key…" — it stores in Keychain.
export COSHOT_CEREBRAS_KEY=csk-your-key-here
